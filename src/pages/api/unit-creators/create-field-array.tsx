import CreateFieldArray from '@components/pages/unit-creators/create-field-array';
import React, { memo } from 'react';

interface IProps {}

const CreateFieldArrayPage = ({}: IProps) => {
  return <CreateFieldArray />;
};

export default memo(CreateFieldArrayPage);
