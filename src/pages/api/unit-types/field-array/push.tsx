import React, { memo } from 'react';
import MainLayout from '@components/main-layout';

interface IProps {}

const Push = ({}: IProps) => {
  return (
    <div>Push</div>
  );
};

export default memo(Push);
