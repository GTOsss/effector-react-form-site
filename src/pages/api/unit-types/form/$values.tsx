import React, { memo } from 'react';
import MainLayout from '@components/main-layout';

interface IProps {}

const Values = ({}: IProps) => {
  return (
    <div>Values</div>
  );
};

export default memo(Values);
