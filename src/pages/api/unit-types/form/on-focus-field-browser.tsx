import React, { memo } from 'react';

interface IProps {}

const OnFocusFieldBrowser = ({}: IProps) => {
  return (
    <div>OnFocusFieldBrowser</div>
  );
};

export default memo(OnFocusFieldBrowser);
