import React, { memo } from 'react';
import MainLayout from '@components/main-layout';

interface IProps {}

const Meta = ({}: IProps) => {
  return (
    <div>Meta</div>
  );
};

export default memo(Meta);
