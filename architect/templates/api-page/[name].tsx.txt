import React, { memo } from 'react';

interface IProps {}

const {{toCamelCase(name)}} = ({}: IProps) => {
  return (
    <div>{{toCamelCase(name)}}</div>
  );
};

export default memo({{toCamelCase(name)}});
