import React, { memo } from 'react';
import MainLayout from '@components/main-layout';

interface IProps {}

const SetIn = ({}: IProps) => {
  return (
    <div>SetIn</div>
  );
};

export default memo(SetIn);
