import GetNameStr from '@components/pages/unit-types/form/get-name-str';
import React, { memo } from 'react';

interface IProps {}

const GetNameStrPage = ({}: IProps) => {
  return <GetNameStr />;
};

export default memo(GetNameStrPage);
