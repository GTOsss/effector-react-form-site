import React, { memo } from 'react';

interface IProps {}

const ResetOuterFieldStateFlags = ({}: IProps) => {
  return (
    <div>ResetOuterFieldStateFlags</div>
  );
};

export default memo(ResetOuterFieldStateFlags);
