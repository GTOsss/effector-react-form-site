import React, { memo } from 'react';
import MainLayout from '@components/main-layout';

interface IProps {}

const Remove = ({}: IProps) => {
  return (
    <div>Remove</div>
  );
};

export default memo(Remove);
