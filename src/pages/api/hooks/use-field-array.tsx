import React, { memo } from 'react';
import MainLayout from '@components/main-layout';

interface IProps {}

const UseFieldArray = ({}: IProps) => {
  return (
    <div>UseFieldArray</div>
  );
};

export default memo(UseFieldArray);
