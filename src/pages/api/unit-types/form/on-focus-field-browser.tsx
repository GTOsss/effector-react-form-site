import OnFocusFieldBrowser from '@components/pages/unit-types/form/on-focus-field-browser';
import React, { memo } from 'react';

interface IProps {}

const OnFocusFieldBrowserPage = ({}: IProps) => {
  return <OnFocusFieldBrowser />;
};

export default memo(OnFocusFieldBrowserPage);
