import React, { memo } from 'react';
import MainLayout from '@components/main-layout';

interface IProps {}

const Submit = ({}: IProps) => {
  return (
    <div>Submit</div>
  );
};

export default memo(Submit);
