import SetSubmitted from '@components/pages/unit-types/form/set-submitted';
import React, { memo } from 'react';

interface IProps {}

const SetSubmittedPage = ({}: IProps) => {
  return <SetSubmitted />;
};

export default memo(SetSubmittedPage);
