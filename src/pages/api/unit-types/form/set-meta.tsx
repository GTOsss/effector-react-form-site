import SetMeta from '@components/pages/unit-types/form/set-meta';
import React, { memo } from 'react';

interface IProps {}

const SetMetaPage = ({}: IProps) => {
  return <SetMeta />;
};

export default memo(SetMetaPage);
