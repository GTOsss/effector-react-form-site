import React, { memo } from 'react';
import MainLayout from '@components/main-layout';

interface IProps {}

const SetErrorsInlineState = ({}: IProps) => {
  return (
    <div>SetErrorsInlineState</div>
  );
};

export default memo(SetErrorsInlineState);
