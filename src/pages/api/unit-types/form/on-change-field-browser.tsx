import React, { memo } from 'react';

interface IProps {}

const OnChangeFieldBrowser = ({}: IProps) => {
  return (
    <div>OnChangeFieldBrowser</div>
  );
};

export default memo(OnChangeFieldBrowser);
