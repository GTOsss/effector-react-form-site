import React, { memo } from 'react';
import MainLayout from '@components/main-layout';

interface IProps {}

const Name = ({}: IProps) => {
  return (
    <div>Name</div>
  );
};

export default memo(Name);
