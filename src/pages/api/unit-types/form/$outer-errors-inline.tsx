import OuterErrorsInline from '@components/pages/unit-types/form/$outer-errors-inline';
import React, { memo } from 'react';

interface IProps {}

const OuterErrorsInlinePage = ({}: IProps) => {
  return <OuterErrorsInline />;
};

export default memo(OuterErrorsInlinePage);
