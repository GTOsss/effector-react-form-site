import Values from '@components/pages/unit-types/form/$values';
import React, { memo } from 'react';

interface IProps {}

const ValuesPage = ({}: IProps) => {
  return <Values />;
};

export default memo(ValuesPage);
