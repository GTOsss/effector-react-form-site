import React, { memo } from 'react';
import MainLayout from '@components/main-layout';

interface IProps {}

const Form = ({}: IProps) => {
  return (
    <div>Form</div>
  );
};

export default memo(Form);
