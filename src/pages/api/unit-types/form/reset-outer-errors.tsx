import React, { memo } from 'react';
import MainLayout from '@components/main-layout';

interface IProps {}

const ResetOuterErrors = ({}: IProps) => {
  return (
    <div>ResetOuterErrors</div>
  );
};

export default memo(ResetOuterErrors);
