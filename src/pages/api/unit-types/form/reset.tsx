import React, { memo } from 'react';
import MainLayout from '@components/main-layout';

interface IProps {}

const Reset = ({}: IProps) => {
  return (
    <div>Reset</div>
  );
};

export default memo(Reset);
