export const nameHelperExample = `import { createNameHelper } from 'effector-react-form';

const { getPath, getStr } = createNameHelper();
`;

export const getPathExample = `getPath('user', 'name') => ['user', 'name'];
`;

export const getStrExample = `getStr('user', 'name') => 'user.name';
`;

