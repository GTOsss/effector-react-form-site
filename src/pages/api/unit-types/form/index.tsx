import Form from '@components/pages/unit-types/form';
import React, { memo } from 'react';

interface IProps {}

const FormPage = ({}: IProps) => {
  return <Form />;
};

export default memo(FormPage);
