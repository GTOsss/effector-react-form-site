import React, { memo } from 'react';
import MainLayout from '@components/main-layout';

interface IProps {}

const Index = ({}: IProps) => {
  return (
    <div>Index</div>
  );
};

export default memo(Index);
