import Remove from '@components/pages/unit-types/field-array/remove';
import React, { memo } from 'react';

interface IProps {}

const RemovePage = ({}: IProps) => {
  return <Remove />;
};

export default memo(RemovePage);
