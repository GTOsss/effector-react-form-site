import React, { memo } from 'react';
import MainLayout from '@components/main-layout';

interface IProps {}

const AllFormState = ({}: IProps) => {
  return (
    <div>AllFormState</div>
  );
};

export default memo(AllFormState);
